The directory structure is organized as follows:
T1D/
Contains the input files required for eQTL analysis. These files serve as example inputs for running the pipeline on type 1 diabetes (T1D) data.

.h5ad file
The included .h5ad file is a processed single-cell dataset. It has already undergone clustering and cell-type annotation, and is used as the single-cell reference input for the analysis.
